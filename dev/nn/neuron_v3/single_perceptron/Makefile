SIM=icarus
TOPLEVEL_LANG = verilog
VERILOG_SOURCES = $(shell pwd)/single_perceptron_tb.v
TOPLEVEL = single_perceptron_tb
MODULE = single_perceptron_test

include $(shell cocotb-config --makefiles)/Makefile.sim
